{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd apps/frontend && bun run build",
  "outputDirectory": "apps/frontend/.output",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ],
  "functions": {
    "apps/api/api/index.ts": {
      "runtime": "edge"
    }
  }
}
